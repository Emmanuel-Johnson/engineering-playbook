- Load factor is the ratio of stored elements to the total size of the hash table.
- Formula: Load Factor = n / m
- A high load factor increases collisions, so performance becomes slower.

- n = number of stored elements (keys/items)
- m = total size of the hash table (number of buckets/slots)