- A controlled component in React is a form element whose value is controlled by React state.
- The input value and changes are managed using useState and onChange.
- This gives better control over form data and validation.


- An uncontrolled component in React stores form data in the DOM instead of React state.
- We use ref to access the input value directly when needed.
- It is simpler for small forms but gives less control compared to controlled components.